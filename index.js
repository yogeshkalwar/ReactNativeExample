/**
 * @format
 */

import {AppRegistry} from 'react-native';
import GameExample from './GameExample';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => GameExample);
